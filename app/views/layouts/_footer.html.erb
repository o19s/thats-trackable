<!-- fixed tabbed footer -->
<div class="navbar navbar-default navbar-fixed-bottom">
  <div class="container">
    <div class="bootcards-desktop-footer clearfix">
      <p class="pull-left">Made with <i class="fa fa-heart"></i> in Charlottesville, Virginia</p>
    </div>
    <% if @current_runner %>
    <div class="btn-group">
      <a href="<%= root_path %>" class="btn btn-default" data-pjax="#main" data-title="Dashboard">
        <i class="fa fa-2x fa-dashboard"></i>
        Dashboard
      </a>
      <a href="<%= runner_runs_path(@current_runner) %>" class="btn btn-default" data-pjax="#main" data-title="My Runs">
        <i class="fa fa-2x fa-history"></i>
        My Runs
      </a>
      <a href="<%= teammates_path %>" class="btn btn-default active" data-pjax="#main" data-title="Teammates!">
        <i class="fa fa-2x fa-bullhorn"></i>
        Team mates!
      </a>
      <% if runner_role == 'Coach' %>
      <a href="<%= runners_path %>" class="btn btn-default active" data-pjax="#main" data-title="Runners!">
        <i class="fa fa-2x glyphicon glyphicon-user"></i>
        Admin Runners
      </a>
      <% end %>
    </div>
    <% end %>
  </div>
</div>
