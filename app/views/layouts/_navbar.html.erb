<nav class="navbar navbar-default">
  <div class="container-fluid">

    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <strong class="navbar-brand">Run Tracker</strong>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <li>
              <%= link_to "Runners", runners_path %>
            </li>
            <% if runner_role == 'Coach' %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Administration <span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <li>
                    <%= link_to "Groups", groups_path %>
                  </li>
                  <li class="divider"></li>
                  <% Group.all.each do |group|%>
                    <li><%= link_to "Run Group #{group.name}", group_planned_runs_path(group) %></li>
                  <% end %>

                </ul>
              </li>

            <% end %>

            <li>
              <%= link_to "My Runs", runner_runs_path(current_runner) %>
            </li>
            <li>
              <%= link_to "Today's Run", today_path %>
            </li>
          </ul>


          <ul class="nav navbar-nav navbar-right">
            <% if logged_in? %>
              <li>
                <span class="navbar-text">Signed in as <%= current_runner.name %></span>
              </li>
              <li>
                <%= link_to 'Link to Facebook', '/auth/facebook' if current_runner.facebook_user.nil?%>
              </li>
                <% if runner_role == 'Coach' %>
                    <li class="dropdown">
                      <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Settings <span class="caret"></span></a>
                      <ul class="dropdown-menu" role="menu">
                        <li>
                          <%= link_to "Change Password", runner_password_path(current_runner) %>
                        </li>
                        <li>
                          <%= link_to "Change Runners' Password", runners_path %>
                        </li>
                      </ul>
                    </li>
                <% else %>
                    <li>
                      <%= link_to "Settings", runner_password_path(current_runner) %>
                    </li>
                <% end %>
              <li>
                <%= link_to "Log Out", logout_path, method: :delete, class: "btn btn-default btn-danger" %>
              </li>
            <% end %>
          </ul>

        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>